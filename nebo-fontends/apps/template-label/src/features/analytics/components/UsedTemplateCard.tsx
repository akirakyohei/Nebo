import { Card } from "@mui/material";
import { UsedPaperType, UsedTemplates } from "../../../types";

interface Props {
  usedTemplates: UsedTemplates;
}

export const UsedTemplateCard = ({ usedTemplates }: Props) => {
  return <Card>sdfsfd</Card>;
};
