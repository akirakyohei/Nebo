import { Card } from "@mui/material";
import { UsedPaperTypes } from "../../../types";

interface Props {
  usedPaperTypes: UsedPaperTypes;
}

export const UsedPaperTypeCard = ({}: Props) => {
  return <Card>sdfsfd</Card>;
};
